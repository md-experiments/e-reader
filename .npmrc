# kokoro-js → onnxruntime-node's postinstall downloads large CUDA binaries by
# default. We only use onnxruntime-web (browser); skip the CUDA download so
# installs (local, CI, Vercel) stay fast and work without GPU binaries.
onnxruntime-node-install-cuda=skip
